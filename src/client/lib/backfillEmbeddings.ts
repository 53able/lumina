/**
 * Embedding が無い論文をバックグラウンドで Embedding API で補完する処理
 *
 * Step 1 Red 用: スタブのみ。テストは「fetchEmbedding/addPaper が呼ばれる」で失敗する。
 * Step 3 Green で実装する。
 */

export const runBackfillEmbeddings = async (
  _papers: unknown[],
  _deps: unknown
): Promise<void> => {
  /* 未実装: テストがアサーションで失敗する */
};
